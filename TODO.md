# TODO - Modernize Nutrition-Scanner web pages

- [ ] Update `styles.css` with a modern design system (CSS variables, scoped selectors, better nav/hero/card/button styling; remove problematic global `div` rules).
- [ ] Refactor `Index.html` to reduce inline layout styles, fix stray/invalid markup, and apply semantic classes for sections.
- [ ] Ensure modal still works (`script.js`) and that “Start Scanning” triggers the modal correctly.
- [ ] (Optional) Reduce inline styling in `ai.html` by reusing the same CSS classes.
- [ ] Quick verification: open `Index.html` and `ai.html` in browser to confirm layout + modal + no broken styling.



